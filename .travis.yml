language: java
jdk:
  - openjdk8

install: true
script: 
  - mvn clean install
  - bash <(curl -s https://codecov.io/bash)
  - mvn deploy

cache:
  directories:
  - $HOME/.m2

deploy:
  -
   on:
     branch: master
   provider: tower
   
